# Core dependencies (installed and working)
Django>=5.2.8
djangorestframework>=3.16.1
django-cors-headers>=4.9.0
Pillow>=12.0.0

# Optional: PostgreSQL support (only needed if using PostgreSQL database)
# Uncomment the line below if you want to use PostgreSQL instead of SQLite
# psycopg2-binary>=2.9.9

# Optional: JWT authentication (not currently used, but available for future)
# djangorestframework-simplejwt>=5.3.0

# Optional: Environment variables (if using .env file)
# python-decouple>=3.8
